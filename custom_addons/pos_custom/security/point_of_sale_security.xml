<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Categoría -->
  <record model="ir.module.category" id="base.module_category_sales_point_of_sale">
    <field name="description">
      Helps you get the most out of your points of sale with fast sale
      encoding, simplified payment mode encoding, automatic picking lists
      generation and more.
    </field>
    <field name="sequence">21</field>
  </record>

  <!-- Grupos -->
  <record id="group_pos_user" model="res.groups">
    <field name="name">User</field>
    <field name="category_id" ref="base.module_category_sales_point_of_sale"/>
  </record>
  <record id="group_pos_manager" model="res.groups">
    <field name="name">Administrator</field>
    <field name="category_id" ref="base.module_category_sales_point_of_sale"/>
    <field name="implied_ids" eval="[
      (4, ref('group_pos_user')),
      (4, ref('stock.group_stock_user'))
    ]"/>
  </record>

  <data noupdate="1">

    <!-- 1) Los contadores de cuenta ven todos los extractos -->
    <record id="rule_pos_bank_statement_account_user" model="ir.rule">
      <field name="name">POS: Bank Statements (Accountant)</field>
      <field name="model_id" ref="account.model_account_bank_statement"/>
      <field name="groups" eval="[(4, ref('account.group_account_invoice'))]"/>
      <field name="domain_force">[(1,'=',1)]</field>
    </record>

    <!-- 2) El usuario POS solo ve las líneas generadas por una sesión POS -->
    <record id="rule_pos_bank_statement_line_user" model="ir.rule">
      <field name="name">POS: Only POS lines</field>
      <field name="model_id" ref="account.model_account_bank_statement_line"/>
      <field name="groups" eval="[(4, ref('group_pos_user'))]"/>
      <!-- Aquí usamos statement_id.pos_session_id -->
      <field name="domain_force">[('statement_id.pos_session_id','!=',False)]</field>
    </record>

    <!-- 3) El contable ve todas las líneas -->
    <record id="rule_pos_bank_statement_line_account_user" model="ir.rule">
      <field name="name">POS: Bank Statement Lines (Accountant)</field>
      <field name="model_id" ref="account.model_account_bank_statement_line"/>
      <field name="groups" eval="[(4, ref('account.group_account_invoice'))]"/>
      <field name="domain_force">[(1,'=',1)]</field>
    </record>

    <!-- Multi-company: cada regla para cada modelo POS -->
    <record id="rule_pos_multi_company" model="ir.rule">
      <field name="name">POS: PoS Orders multi-company</field>
      <field name="model_id" ref="point_of_sale.model_pos_order"/>
      <field name="groups" eval="[
        (4, ref('group_pos_user')),
        (4, ref('group_pos_manager'))
      ]"/>
      <field name="domain_force">[('company_id','in',company_ids)]</field>
    </record>
    <record id="rule_pos_order_line_multi_company" model="ir.rule">
      <field name="name">POS: PoS Order Lines multi-company</field>
      <field name="model_id" ref="point_of_sale.model_pos_order_line"/>
      <field name="groups" eval="[
        (4, ref('group_pos_user')),
        (4, ref('group_pos_manager'))
      ]"/>
      <field name="domain_force">[('company_id','in',company_ids)]</field>
    </record>
    <record id="rule_pos_session_multi_company" model="ir.rule">
      <field name="name">POS: PoS Sessions multi-company</field>
      <field name="model_id" ref="point_of_sale.model_pos_session"/>
      <field name="groups" eval="[
        (4, ref('group_pos_user')),
        (4, ref('group_pos_manager'))
      ]"/>
      <field name="domain_force">[('config_id.company_id','in',company_ids)]</field>
    </record>
    <record id="rule_pos_config_multi_company" model="ir.rule">
      <field name="name">POS: PoS Config multi-company</field>
      <field name="model_id" ref="point_of_sale.model_pos_config"/>
      <field name="groups" eval="[
        (4, ref('group_pos_user')),
        (4, ref('group_pos_manager'))
      ]"/>
      <field name="domain_force">[('company_id','in',company_ids)]</field>
    </record>
    <record id="rule_pos_order_report_multi_company" model="ir.rule">
      <field name="name">POS: Order Analysis multi-company</field>
      <field name="model_id" ref="point_of_sale.model_report_pos_order"/>
      <field name="groups" eval="[
        (4, ref('group_pos_user')),
        (4, ref('group_pos_manager'))
      ]"/>
      <field name="domain_force">[('company_id','in',company_ids)]</field>
    </record>
    <record id="rule_pos_payment_method_multi_company" model="ir.rule">
      <field name="name">POS: Payment Methods multi-company</field>
      <field name="model_id" ref="point_of_sale.model_pos_payment_method"/>
      <field name="groups" eval="[
        (4, ref('group_pos_user')),
        (4, ref('group_pos_manager'))
      ]"/>
      <field name="domain_force">[('company_id','in',company_ids)]</field>
    </record>
    <record id="rule_pos_payment_multi_company" model="ir.rule">
      <field name="name">POS: Payments multi-company</field>
      <field name="model_id" ref="point_of_sale.model_pos_payment"/>
      <field name="groups" eval="[
        (4, ref('group_pos_user')),
        (4, ref('group_pos_manager'))
      ]"/>
      <field name="domain_force">[('company_id','in',company_ids)]</field>
    </record>

    <!-- Facturas POS visibles sólo por usuarios POS -->
    <record id="rule_invoice_pos_user" model="ir.rule">
      <field name="name">Invoice: Only POS invoices</field>
      <field name="model_id" ref="account.model_account_move"/>
      <field name="groups" eval="[(4, ref('group_pos_user'))]"/>
      <field name="domain_force">[('pos_order_ids','!=',False)]</field>
    </record>

    <!-- Asignar al admin root el grupo POS manager -->
    <record id="base.default_user" model="res.users">
      <field name="groups_id" eval="[(4, ref('point_of_sale.group_pos_manager'))]"/>
    </record>

  </data>
</odoo>
